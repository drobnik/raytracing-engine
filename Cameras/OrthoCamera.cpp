#include "OrthoCamera.h"

EngineImage OrthoCamera::renderScene(std::vector<std::unique_ptr<Primitive>> &objs) {
    return EngineImage();
}

Ray OrthoCamera::spawnRay(const Vector3 &point) {
    return Ray();
}